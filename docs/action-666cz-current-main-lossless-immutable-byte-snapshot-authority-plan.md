# Action 666CZ — current-main lossless immutable byte snapshot authority plan

## Planning authority

Action 666CZ starts from protected GitHub main commit
`7280f5a6a7317f495dd8ffccdd8df609203026f5`, tree
`944a9fe64992f115a514070e3a0ff6e5df26c5a1`. This is the ordinary merge of
PR #118 / Action 666CY. Exact-main push CI run `32277517623` completed
successfully with all six provider-free shards and the protected aggregate
green. GitHub deployments for the commit were empty.

Historical PR #72 / Actions 666CH and 666CI are non-authority design context
only. Their source, fixtures, tests, digests, review conclusions and successor
objects are not imported. This is a fresh current-main implementation built
only against delivered 666CY.

## Bounded objective

Create one additive, server-only, private snapshot-authority boundary which:

1. invokes the delivered 666CY byte snapshot only after literal enable and
   kill-switch gates;
2. grants current-process authority only to an exact successful 666CY result
   produced and registered by the originating 666CZ harness session;
3. binds each returned result shell to that exact session in module-private
   weak registries before publication;
4. requires an independent deterministic rebuild and exact deep equality for
   verification;
5. rejects copied, cloned, rebuilt, cross-harness or merely self-consistent
   result shells before trusting their fields; and
6. exposes only a recursively frozen integrity-only readback with provenance,
   trust, admission and verifier authority permanently false.

The Action adds no callback, dependency reader, persistence, provider,
database, route, ranking effect, training, promotion, broker action or
production deployment.

## Required verification

- literal default-off and kill-switch paths perform zero input, predecessor,
  authority, rebuild, readback or digest work;
- successful authority requires exact 666CY `integrity_only` output and its
  private originating 666CZ session;
- shallow copies, deep clones, reconstructed shells and cross-harness results
  fail before result-property introspection;
- result verification rebuilds from the supplied raw request and requires exact
  deep equality with the registered result;
- caller mutation after publication cannot alter a result, evidence, readback
  or counter snapshot;
- public readback never grants runtime provenance or verifier authority;
- hostile proxies, accessors and post-import collection, reflection, freezing,
  hash and array primordial mutation remain structured and never throw from a
  public entry;
- five normative artifacts are byte-pinned by a separate fail-closed manifest;
- both focused tests are registered exactly once in Action 660J without a
  workflow or protected-check identity change.

## Delivery sequence

Freeze the candidate scope, run focused and predecessor regressions,
TypeScript, lint, manifest mutation and security scans, then obtain an
independent exact-head read-only review. Publication of a Draft PR requires
explicit operator approval. Merge requires a later approval naming the exact
PR and head, followed by successful exact-main CI.

No production deploy, provider action, Supabase/database action, broker action,
training, promotion or runtime activation is authorized.
