# AI-02.20 — proxy-route transport decision

**Status:** `draft_branch_only_not_executed` on 2026-09-08.

## Bounded objective

Perform at most one new, separately contained deploy-preview route attestation
only after the AI-02.18 `401` boundary has been identified. This Draft branch
introduces no provider, database, source, evaluator, application-runtime,
broker or production authority and must never merge to `main`.

## Newly verified attribution

The exact AI-02.18 deploy was built from
`ef89a7d4148f02da0dc92c169a1652e55582996b`. Its temporary custom function
was configured at `/api/diagnostics/ai02-preview-route-attestation`, while the
same commit's `proxy.ts` applies to that path and requires an application
session for every API route outside its explicit public allowlist. The path is
not listed there, so the proxy's fixed HTTP `401` session-required response is
the attributable pre-function layer.

Read-only Netlify inspection independently rules out the platform controls
that could otherwise explain the result: production and Deploy Preview
visibility are `Public`; unpublished deploy traffic is `Allow all traffic`
with no geographic or IP restrictions; and WAF is disabled. The exact deploy
reported one Edge function. Netlify documents that its Edge Functions view
contains only function `console` output; the historical deploy-scoped log was
empty, which neither creates an alternative cause nor contradicts the source
attribution.

## Fresh, bounded transport design

This branch's temporary function uses exactly
`/api/automation/ai02-proxy-route-attestation`. That path is already passed
through by `proxy.ts`'s existing `/api/automation/` allowlist, so the new
route neither changes proxy behavior nor weakens visitor access.

After local verification and unchanged Ready Full CI, at most one request may
be made under all of these constraints:

- the PR stays Draft and branch-only;
- one new random `AI02_PROXY_ROUTE_ATTESTATION_TOKEN` exists only in the
  matching branch-deploy function scope;
- one `POST` supplies only that token in
  `x-ai02-proxy-route-attestation-token`;
- the function accepts only the `trade-vl` deploy-preview context and returns
  a fixed no-provider/no-database receipt;
- any non-200 or non-exact receipt stops the slice immediately;
- remove the token, close the PR, delete its remote/local branch and remove
  its worktree after that single result.

No request may target AI-02.17, AI-02.18, an application route, a provider,
Supabase, a writer, an evaluator, a broker, a runtime or production.

## Governance record

```text
action_or_decision_id: AI-02.20
bounded_objective: verify exactly one proxy-allowlisted custom deploy-preview function route
milestone_or_product_outcome: resolve the preview transport prerequisite for a later separately selected AI-02 decision
threat_or_delivery_risk_reduced: repeating a consumed route or weakening global access controls after a now-attributable 401
blocked_by: Ready Full CI, exact Draft preview identity, branch-only secret presence and one-request execution containment
unblocks: only a later fresh decision; never AI-02.17/18, provider access, source creation, persistence, runtime, broker or production
authority_boundary: temporary branch-preview transport proof only; no data/provider/runtime authority
required_evidence: focused local test, unchanged Ready Full CI, exact preview identity and fixed redacted receipt
focused_verification: proxy-allowlist source assertion and function boundary tests
residual_risks: a 200 proves only this route/token transport, not staging, data, provider or application capability
autonomous_governance_controller: codex
delivery_automation: Draft PR only, with immediate secret/PR/branch/worktree cleanup after one result
independent_machine_verification: focused test plus unchanged protected Ready Full CI and later exact-main documentation closeout
decision_policy_version: roadmap-operating-governance-v1
stop_go_or_closeout_trigger: non-200, non-exact receipt, mismatched preview, missing CI or missing token
rollback_or_containment: no state is written; all temporary material is deleted after one result
```
