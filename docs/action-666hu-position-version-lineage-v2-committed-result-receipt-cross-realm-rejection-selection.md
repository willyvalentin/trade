# Action 666HU — V2 committed-result receipt cross-realm rejection selection

## Bounded selection

This source-only selection chooses exactly one future independent review of the
completed immutable V2 receipt equivalence comparator: frozen in-memory
receipt-shaped objects created in a foreign JavaScript realm. The selected
review may prove that foreign-realm material fails closed in either argument
slot with fresh dedicated comparator errors.

## Required future observations

The selected review may invoke the comparator three times in each argument
order with a locally valid immutable receipt and a separately created frozen
foreign-realm receipt-shaped object. It must prove stable rejection, the
established dedicated error name and message, and no input or cross-error
aliasing. It may not adapt, normalize or import foreign-realm material.

## Containment

This selection changes no comparator source and creates no receipt consumer,
storage, caller, transport, credential, identity or owner resolution,
database or writer operation, provider, broker, route/UI, deployment or
runtime binding. It does not change CI semantics, required checks, branch
protection, Netlify or the POC policy. No CI deduplication is authorized.
Ready and exact-main six-shard Full CI remain mandatory. Only the separately
bounded review may follow.
