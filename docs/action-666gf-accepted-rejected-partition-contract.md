# Action 666GF — Accepted-versus-rejected exit-explanation partition

## Decision

Action 666GF independently reviews the complete finite partition exposed by the
source-only Action 666GA exit-decision explanation projection. Exactly seven
declared classification tuples project one fixed Swedish advisory string. Every
other exact, closed tuple is rejected as
`unsupported_decision_classification`; malformed shapes remain
`invalid_input_shape`.

## Partition boundary

The review enumerates the 196-tuple known vocabulary cross-product: four known
statuses, seven known reasons and seven known safe-integer priorities. It
confirms that exactly the seven declared triples project and all 189 remaining
well-formed tuples receive the canonical unsupported rejection. It also checks
one-field status, reason and priority perturbations around each accepted tuple,
plus an extra field, accessor and fractional priority malformed boundary.

No input can cross from the rejected partition into advisory output by merely
being close to an accepted triple. Projected results remain advisory-only with
`runtime_wired: false` and `side_effects_performed: false`; rejected outcomes
retain the canonical null-only payload established by Action 666GE. No
implementation change is necessary because the frozen table already enforces
this partition.

## Delivery decision

This is a source-only review. It creates no evaluator caller and changes no
workflow, required check, branch protection, Netlify configuration or Full CI
deduplication policy. No data, provider, secret, transport, database, writer,
route/UI, broker or execution authority is added. Ready and exact-main
six-shard Full CI remain mandatory.

The next bounded action is `ACTION_666GG`: a source-only table-definition
integrity review for the same projection. It must preserve this partition and
may not add runtime wiring.
