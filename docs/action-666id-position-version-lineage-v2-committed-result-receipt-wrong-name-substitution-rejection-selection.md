# Action 666ID — V2 committed-result receipt wrong-name substitution rejection selection

## Bounded selection

Action 666ID is a source-only selection of one future independent review of the
completed immutable V2 committed-result receipt equivalence comparator. The
future review may invoke only that comparator with ordinary valid frozen local
receipt controls and one separately created frozen local ordinary
receipt-shaped object.
The review fixture must retain direct local Object.prototype and exactly five
enumerable, immutable normal own data fields with canonical scalar values, but
must replace the canonical `canonicalCommandDigest` own data key one-for-one
with an enumerable `legacyCanonicalCommandDigest` own data key carrying the
same canonical 64-character digest scalar.

## Required observations

The future review must prove that `Reflect.ownKeys` sees exactly five string
own data keys: the four unaffected canonical names and
`legacyCanonicalCommandDigest`, with no `canonicalCommandDigest` key. That
keeps the key count unchanged while separately exercising the comparator's
exact-name-membership boundary.
It must invoke the comparator three times in each argument order. Every
comparison must reject with a fresh dedicated comparator error carrying the
established public name and message. No error may alias an input, a positive
verdict or a previous error. Two ordinary valid local frozen receipts must
remain admissible with a fresh frozen scalar-only verdict.

The future review may create no proxy, accessor, symbol, foreign-realm
material, null or custom prototype, prototype mutation, coercion hook,
adaptation, normalization or import. It may not add an extra key or use a
four-key omitted-field fixture; that is a separate possible future boundary.

## Containment

This selection changes no comparator source and does not decode a result or
command, inspect or bind a caller, or create a receipt consumer, storage,
transport, credential, identity or owner resolution, database or writer
operation, provider, broker, route/UI, deployment or runtime binding. It does
not change CI semantics, required checks, branch protection, Netlify or the
POC policy. No CI deduplication is authorized. Ready and exact-main six-shard
Full CI remain mandatory. Only the separately bounded review may follow.
